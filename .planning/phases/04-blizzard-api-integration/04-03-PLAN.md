---
phase: 04-blizzard-api-integration
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - tests/Fixtures/blizzard_commodities.json
  - tests/Feature/BlizzardApi/BlizzardTokenServiceTest.php
  - tests/Feature/BlizzardApi/PriceFetchActionTest.php
autonomous: true
requirements:
  - DATA-05

must_haves:
  truths:
    - "Token cache hit test confirms second getToken() call makes zero HTTP requests"
    - "Token fetch test confirms Basic Auth header (not query params) on POST to oauth.battle.net/token"
    - "Commodity fetch test confirms Authorization: Bearer header on GET to commodities endpoint"
    - "Commodity fetch test confirms namespace=dynamic-us query parameter"
    - "Filtering test confirms unwatched items are excluded from returned array"
    - "Error tests confirm RuntimeException on 401 and 5xx responses"
  artifacts:
    - path: "tests/Fixtures/blizzard_commodities.json"
      provides: "Shared fixture with 5-10 realistic commodity listings"
      contains: "auctions"
    - path: "tests/Feature/BlizzardApi/BlizzardTokenServiceTest.php"
      provides: "Token caching, Basic Auth, and error handling tests"
      contains: "BlizzardTokenService"
    - path: "tests/Feature/BlizzardApi/PriceFetchActionTest.php"
      provides: "Bearer header, namespace param, filtering, and error tests"
      contains: "PriceFetchAction"
  key_links:
    - from: "tests/Feature/BlizzardApi/BlizzardTokenServiceTest.php"
      to: "app/Services/BlizzardTokenService.php"
      via: "app() resolution with Http::fake()"
      pattern: "Http::fake.*BlizzardTokenService"
    - from: "tests/Feature/BlizzardApi/PriceFetchActionTest.php"
      to: "app/Actions/PriceFetchAction.php"
      via: "app() resolution with Http::fake()"
      pattern: "Http::fake.*PriceFetchAction"
    - from: "tests/Feature/BlizzardApi/PriceFetchActionTest.php"
      to: "tests/Fixtures/blizzard_commodities.json"
      via: "file_get_contents for fixture loading"
      pattern: "blizzard_commodities\\.json"
---

<objective>
Write Pest feature tests for BlizzardTokenService and PriceFetchAction using Http::fake() with JSON fixture files, covering happy paths and error scenarios.

Purpose: Validate that the token service caches correctly, uses Basic Auth (not query params), and that the fetch action sends Bearer headers and filters results — all without hitting the live Blizzard API.
Output: JSON fixture file in `tests/Fixtures/`, two Pest test files in `tests/Feature/BlizzardApi/` with full DATA-05 coverage.
</objective>

<execution_context>
@/Users/lancethompson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lancethompson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-blizzard-api-integration/04-RESEARCH.md
@.planning/phases/04-blizzard-api-integration/04-01-SUMMARY.md
@.planning/phases/04-blizzard-api-integration/04-02-SUMMARY.md

<interfaces>
<!-- Classes under test -->

From app/Services/BlizzardTokenService.php:
```php
namespace App\Services;

class BlizzardTokenService
{
    public function getToken(): string;
}
```

From app/Actions/PriceFetchAction.php:
```php
namespace App\Actions;

class PriceFetchAction
{
    public function __construct(private readonly BlizzardTokenService $tokenService);
    public function __invoke(array $itemIds): array;
}
```

<!-- Blizzard response structure for fixtures -->
```json
{
  "auctions": [
    {"id": 1, "item": {"id": 224025}, "unit_price": 150000, "quantity": 200, "time_left": "VERY_LONG"}
  ]
}
```

<!-- Http::fake() pattern from research -->
```php
Http::fake([
    'oauth.battle.net/token' => Http::response(
        ['access_token' => 'fake-token', 'token_type' => 'bearer', 'expires_in' => 86400], 200
    ),
    '*.api.blizzard.com/data/wow/auctions/commodities' => Http::response(
        json_decode(file_get_contents(base_path('tests/Fixtures/blizzard_commodities.json')), true), 200
    ),
]);
```

<!-- Http::assertSent() pattern -->
```php
Http::assertSent(function (Request $request) {
    return str_contains($request->url(), 'auctions/commodities')
        && $request->hasHeader('Authorization', 'Bearer fake-token');
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create blizzard_commodities.json fixture and BlizzardTokenServiceTest</name>
  <files>tests/Fixtures/blizzard_commodities.json, tests/Feature/BlizzardApi/BlizzardTokenServiceTest.php</files>
  <action>
**Step 1: Create fixture file** `tests/Fixtures/blizzard_commodities.json`

Create directory `tests/Fixtures/` if it does not exist. Write a JSON file with realistic Blizzard commodities structure containing 6 auction entries:
- 2 entries for item ID 224025 (Bismuth, a TWW item from catalog seeder) at different prices/quantities
- 2 entries for item ID 210781 (Mycbloom, another TWW catalog item) at different prices/quantities
- 1 entry for item ID 210930 (Aqirite, another TWW catalog item)
- 1 entry for item ID 999999 (unwatched item — used to verify filtering excludes it)

Use realistic copper prices (100000-500000 range), realistic quantities (10-500), and varied `time_left` values (`VERY_LONG`, `LONG`, `MEDIUM`, `SHORT`). Each entry must have `id`, `item.id`, `unit_price`, `quantity`, and `time_left` fields matching the Blizzard response structure.

**Step 2: Create test file** `tests/Feature/BlizzardApi/BlizzardTokenServiceTest.php`

Create directory `tests/Feature/BlizzardApi/` if it does not exist. Use `declare(strict_types=1)`. Add proper `use` statements for `Http`, `Cache`, `Illuminate\Http\Client\Request`.

Write these Pest tests:

1. **`it('fetches a new token via Basic Auth when cache is empty')`**
   - `Cache::forget('blizzard_token')` to clear any cached token
   - `Http::fake()` with `oauth.battle.net/token` returning `{'access_token': 'test-token', 'expires_in': 86400}` status 200
   - Resolve `BlizzardTokenService` from container, call `getToken()`
   - `expect($token)->toBe('test-token')`
   - `Http::assertSent()` verifying the request URL contains `oauth.battle.net/token` AND the request has an `Authorization` header starting with `Basic ` (confirming Basic Auth, not query params)
   - `Http::assertSentCount(1)`

2. **`it('returns cached token without HTTP request on cache hit')`**
   - `Cache::forget('blizzard_token')` then set up `Http::fake()` returning token
   - Call `getToken()` twice on the same service instance
   - Both calls return `'test-token'`
   - `Http::assertSentCount(1)` — only one HTTP request despite two getToken() calls

3. **`it('throws RuntimeException on 401 unauthorized')`**
   - `Cache::forget('blizzard_token')`
   - `Http::fake()` with `oauth.battle.net/token` returning status 401
   - `expect(fn() => $service->getToken())->toThrow(RuntimeException::class)`

4. **`it('throws RuntimeException on 500 server error')`**
   - `Cache::forget('blizzard_token')`
   - `Http::fake()` with `oauth.battle.net/token` returning status 500
   - `expect(fn() => $service->getToken())->toThrow(RuntimeException::class)`

Add a `beforeEach()` that calls `Cache::forget('blizzard_token')` to ensure clean state between tests.
  </action>
  <verify>
    <automated>php artisan test --filter BlizzardTokenServiceTest</automated>
  </verify>
  <done>Fixture file exists with 6 realistic auction entries (including one unwatched item). BlizzardTokenServiceTest has 4 tests: Basic Auth verification, cache hit (assertSentCount(1)), 401 error, and 500 error. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create PriceFetchActionTest with fetch, filtering, and error tests</name>
  <files>tests/Feature/BlizzardApi/PriceFetchActionTest.php</files>
  <action>
Create `tests/Feature/BlizzardApi/PriceFetchActionTest.php` with `declare(strict_types=1)`. Add `use` statements for `Http`, `Cache`, `Illuminate\Http\Client\Request`, `App\Actions\PriceFetchAction`.

Set up a `beforeEach()` that:
- Calls `Cache::forget('blizzard_token')` to clear token cache
- Sets up `Http::fake()` with BOTH endpoints:
  - `oauth.battle.net/token` returning `{'access_token': 'test-token', 'expires_in': 86400}` status 200
  - `*.api.blizzard.com/data/wow/auctions/commodities` returning the fixture file loaded via `json_decode(file_get_contents(base_path('tests/Fixtures/blizzard_commodities.json')), true)`

Write these Pest tests:

1. **`it('sends Authorization Bearer header on commodity fetch')`**
   - Resolve `PriceFetchAction` from container, invoke with `[224025]`
   - `Http::assertSent()` verifying the commodities request has `Authorization: Bearer test-token` header
   - This is the critical success criteria test — token must go in header, never as query param

2. **`it('sends namespace=dynamic-us query parameter')`**
   - Invoke action with `[224025]`
   - `Http::assertSent()` verifying the commodities request URL contains `namespace=dynamic-us` (or use `$request->data()` / URL parsing to confirm the parameter)

3. **`it('filters results to only requested item IDs')`**
   - Invoke action with `[224025, 210781]` (items in the fixture)
   - Result should contain entries for 224025 and 210781 only
   - Result should NOT contain item 999999 (the unwatched item in the fixture)
   - Assert count matches expected (5 entries: 2 for 224025, 2 for 210781, 1 for 210930... wait — only [224025, 210781] requested, so 210930 should also be excluded)
   - `expect(count($result))->toBe(4)` (2 for 224025 + 2 for 210781)
   - Verify all returned entries have `item.id` in `[224025, 210781]`

4. **`it('returns empty array when no watched items match')`**
   - Invoke action with `[111111]` (an ID not in the fixture)
   - `expect($result)->toBe([])`

5. **`it('throws RuntimeException on 500 from commodities endpoint')`**
   - Override `Http::fake()` for commodities to return status 500 (keep token endpoint at 200)
   - `expect(fn() => $action([224025]))->toThrow(RuntimeException::class)`

6. **`it('returns re-indexed array after filtering')`**
   - Invoke action with `[210930]` (only 1 entry in fixture)
   - `expect(array_keys($result))->toBe([0])` — keys start at 0 (not sparse from array_filter)
  </action>
  <verify>
    <automated>php artisan test --filter PriceFetchActionTest</automated>
  </verify>
  <done>PriceFetchActionTest has 6 tests: Bearer header assertion, namespace parameter, item filtering, empty result, 500 error handling, and re-indexed array keys. All tests pass.</done>
</task>

</tasks>

<verification>
1. `php artisan test --filter BlizzardApi` — all tests in both test files pass
2. `php artisan test` — full suite still green (no regressions)
3. `cat tests/Fixtures/blizzard_commodities.json | python3 -m json.tool` — valid JSON with auctions array
4. `grep -r 'Http::assertSent' tests/Feature/BlizzardApi/` — confirms request assertions exist
5. `grep -r 'assertSentCount(1)' tests/Feature/BlizzardApi/BlizzardTokenServiceTest.php` — confirms cache hit test
</verification>

<success_criteria>
- tests/Fixtures/blizzard_commodities.json exists with 6 realistic auction entries including 1 unwatched item
- BlizzardTokenServiceTest covers: Basic Auth, cache hit, 401 error, 500 error (4 tests)
- PriceFetchActionTest covers: Bearer header, namespace param, filtering, empty result, 500 error, re-indexed keys (6 tests)
- All 10 tests pass via `php artisan test --filter BlizzardApi`
- Full test suite passes via `php artisan test` (no regressions)
- Tests use Http::fake() exclusively — no live Blizzard API calls
- Cache::forget() in beforeEach ensures clean state between tests
</success_criteria>

<output>
After completion, create `.planning/phases/04-blizzard-api-integration/04-03-SUMMARY.md`
</output>
