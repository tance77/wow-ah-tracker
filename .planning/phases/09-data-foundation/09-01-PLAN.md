---
phase: 09-data-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/2026_03_05_100000_create_shuffles_table.php
  - database/migrations/2026_03_05_100001_create_shuffle_steps_table.php
  - database/migrations/2026_03_05_100002_add_created_by_shuffle_id_to_watched_items.php
  - app/Models/Shuffle.php
  - app/Models/ShuffleStep.php
  - app/Models/User.php
  - app/Models/WatchedItem.php
autonomous: true
requirements: []

must_haves:
  truths:
    - "Running migrations creates shuffles and shuffle_steps tables with correct columns and FKs"
    - "Shuffle model has steps() relationship ordered by sort_order"
    - "ShuffleStep model has inputCatalogItem() and outputCatalogItem() relationships via blizzard_item_id"
    - "User model has shuffles() relationship"
    - "Deleting a shuffle cascade-deletes its steps at DB level"
    - "Deleting a shuffle triggers orphan cleanup for auto-watched items"
    - "watched_items.created_by_shuffle_id is nullable and set to null on shuffle delete"
  artifacts:
    - path: "database/migrations/2026_03_05_100000_create_shuffles_table.php"
      provides: "shuffles table with user_id FK"
      contains: "Schema::create('shuffles'"
    - path: "database/migrations/2026_03_05_100001_create_shuffle_steps_table.php"
      provides: "shuffle_steps table with yield columns and sort_order"
      contains: "output_qty_min"
    - path: "database/migrations/2026_03_05_100002_add_created_by_shuffle_id_to_watched_items.php"
      provides: "Auto-watch provenance FK on watched_items"
      contains: "created_by_shuffle_id"
    - path: "app/Models/Shuffle.php"
      provides: "Shuffle Eloquent model with relationships and orphan cleanup"
      exports: ["Shuffle"]
    - path: "app/Models/ShuffleStep.php"
      provides: "ShuffleStep Eloquent model with catalog item relationships"
      exports: ["ShuffleStep"]
  key_links:
    - from: "app/Models/Shuffle.php"
      to: "app/Models/ShuffleStep.php"
      via: "steps() HasMany ordered by sort_order"
      pattern: "hasMany\\(ShuffleStep::class\\)->orderBy\\('sort_order'\\)"
    - from: "app/Models/ShuffleStep.php"
      to: "app/Models/CatalogItem.php"
      via: "inputCatalogItem() and outputCatalogItem() BelongsTo"
      pattern: "belongsTo\\(CatalogItem::class"
    - from: "app/Models/Shuffle.php"
      to: "app/Models/WatchedItem.php"
      via: "deleting event orphan cleanup"
      pattern: "static::deleting"
    - from: "app/Models/User.php"
      to: "app/Models/Shuffle.php"
      via: "shuffles() HasMany"
      pattern: "hasMany\\(Shuffle::class\\)"
---

<objective>
Create the database schema and Eloquent models for the shuffles feature.

Purpose: Establish the data foundation that all subsequent v1.1 phases (10-12) build on. This is pure infrastructure -- no UI, no endpoints.
Output: 3 migrations, 2 new models (Shuffle, ShuffleStep), 2 modified models (User, WatchedItem)
</objective>

<execution_context>
@/Users/lancethompson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lancethompson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-data-foundation/09-CONTEXT.md
@.planning/phases/09-data-foundation/09-RESEARCH.md

<interfaces>
<!-- Existing model patterns to follow exactly -->

From app/Models/WatchedItem.php:
```php
use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\Model;
use Illuminate\Database\Eloquent\Relations\BelongsTo;

class WatchedItem extends Model
{
    use HasFactory;

    protected $fillable = [
        'user_id',
        'blizzard_item_id',
        'name',
        'buy_threshold',
        'sell_threshold',
        'profession',
    ];

    public function catalogItem(): BelongsTo
    {
        return $this->belongsTo(CatalogItem::class, 'blizzard_item_id', 'blizzard_item_id');
    }
}
```

From app/Models/User.php:
```php
public function watchedItems(): HasMany
{
    return $this->hasMany(WatchedItem::class);
}
```

From database/migrations/2026_03_04_000000_add_profession_to_watched_items.php (alter table pattern):
```php
return new class extends Migration
{
    public function up(): void
    {
        Schema::table('watched_items', function (Blueprint $table) {
            $table->string('profession')->nullable()->after('sell_threshold');
        });
    }
    public function down(): void
    {
        Schema::table('watched_items', function (Blueprint $table) {
            $table->dropColumn('profession');
        });
    }
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migrations for shuffles, shuffle_steps, and watched_items provenance</name>
  <files>
    database/migrations/2026_03_05_100000_create_shuffles_table.php
    database/migrations/2026_03_05_100001_create_shuffle_steps_table.php
    database/migrations/2026_03_05_100002_add_created_by_shuffle_id_to_watched_items.php
  </files>
  <action>
Create three migration files with timestamps ordered so shuffles runs first, then shuffle_steps, then the watched_items alter.

**Migration 1: create_shuffles_table**
- `id()`, `foreignId('user_id')->constrained()->cascadeOnDelete()`, `string('name')`, `timestamps()`
- No description, notes, tags, or active/inactive columns per user decision

**Migration 2: create_shuffle_steps_table**
- `id()`, `foreignId('shuffle_id')->constrained()->cascadeOnDelete()`
- `unsignedBigInteger('input_blizzard_item_id')` -- MUST be unsignedBigInteger, not unsignedInteger (project convention for all Blizzard IDs)
- `unsignedBigInteger('output_blizzard_item_id')` -- same
- `unsignedInteger('output_qty_min')->default(1)` -- integer, not float per user decision
- `unsignedInteger('output_qty_max')->default(1)` -- integer, not float per user decision
- `unsignedInteger('sort_order')->default(0)`
- `timestamps()`
- Add composite index: `$table->index(['shuffle_id', 'sort_order'])`

**Migration 3: add_created_by_shuffle_id_to_watched_items**
- `foreignId('created_by_shuffle_id')->nullable()->after('profession')->constrained('shuffles')->nullOnDelete()`
- down(): `dropConstrainedForeignId('created_by_shuffle_id')`
- Use `nullOnDelete()` -- when shuffle deleted, FK goes null. Orphan cleanup is in model event, not DB.

All files: `declare(strict_types=1)`, anonymous class pattern matching existing migrations.
  </action>
  <verify>
    <automated>cd /Users/lancethompson/Github/wow-ah-tracker &amp;&amp; php artisan migrate --force 2>&amp;1 | tail -5</automated>
  </verify>
  <done>All three migrations run without error. shuffles and shuffle_steps tables exist with correct column types. watched_items has nullable created_by_shuffle_id FK.</done>
</task>

<task type="auto">
  <name>Task 2: Create Shuffle and ShuffleStep models, update User and WatchedItem</name>
  <files>
    app/Models/Shuffle.php
    app/Models/ShuffleStep.php
    app/Models/User.php
    app/Models/WatchedItem.php
  </files>
  <action>
**Shuffle model (new file: app/Models/Shuffle.php):**
- `declare(strict_types=1)`, namespace `App\Models`
- `use HasFactory`
- `$fillable = ['user_id', 'name']`
- `user(): BelongsTo` -- returns `$this->belongsTo(User::class)`
- `steps(): HasMany` -- returns `$this->hasMany(ShuffleStep::class)->orderBy('sort_order')` (ordering on relationship, not in controller)
- `boot()` static method with `static::deleting()` event for orphan cleanup:
  - Find WatchedItem rows where `created_by_shuffle_id = $shuffle->id`
  - Of those, delete ONLY items whose `blizzard_item_id` is NOT referenced as `input_blizzard_item_id` or `output_blizzard_item_id` on ANY shuffle_step belonging to a DIFFERENT shuffle
  - Manually-watched items (`created_by_shuffle_id = null`) are never touched
  - Use the query pattern from 09-RESEARCH.md Pattern 4 as reference
  - CRITICAL: Use `deleting` (before delete), NOT `deleted` (after). Steps must still exist in DB when orphan check runs because DB cascade fires after model event.

**ShuffleStep model (new file: app/Models/ShuffleStep.php):**
- `declare(strict_types=1)`, namespace `App\Models`
- `use HasFactory`
- `$fillable = ['shuffle_id', 'input_blizzard_item_id', 'output_blizzard_item_id', 'output_qty_min', 'output_qty_max', 'sort_order']`
- `$casts = ['input_blizzard_item_id' => 'integer', 'output_blizzard_item_id' => 'integer', 'output_qty_min' => 'integer', 'output_qty_max' => 'integer', 'sort_order' => 'integer']`
- `shuffle(): BelongsTo` -- returns `$this->belongsTo(Shuffle::class)`
- `inputCatalogItem(): BelongsTo` -- returns `$this->belongsTo(CatalogItem::class, 'input_blizzard_item_id', 'blizzard_item_id')` (mirrors WatchedItem::catalogItem() pattern)
- `outputCatalogItem(): BelongsTo` -- returns `$this->belongsTo(CatalogItem::class, 'output_blizzard_item_id', 'blizzard_item_id')`

**User model (modify app/Models/User.php):**
- Add `shuffles(): HasMany` method returning `$this->hasMany(Shuffle::class)`
- Place alongside existing `watchedItems()` method

**WatchedItem model (modify app/Models/WatchedItem.php):**
- Add `'created_by_shuffle_id'` to `$fillable` array
- Add `createdByShuffle(): BelongsTo` returning `$this->belongsTo(Shuffle::class, 'created_by_shuffle_id')`
  </action>
  <verify>
    <automated>cd /Users/lancethompson/Github/wow-ah-tracker &amp;&amp; php artisan tinker --execute "use App\Models\Shuffle; use App\Models\ShuffleStep; echo 'Shuffle fillable: ' . implode(',', (new Shuffle)->getFillable()) . PHP_EOL; echo 'Step fillable: ' . implode(',', (new ShuffleStep)->getFillable()) . PHP_EOL; echo 'User has shuffles: ' . (method_exists(new \App\Models\User, 'shuffles') ? 'yes' : 'no') . PHP_EOL;"</automated>
  </verify>
  <done>Shuffle and ShuffleStep models exist with correct fillable, casts, and relationships. User has shuffles() method. WatchedItem has created_by_shuffle_id in fillable and createdByShuffle() relationship.</done>
</task>

</tasks>

<verification>
- `php artisan migrate:status` shows all three new migrations as "Ran"
- `php artisan tinker` can instantiate Shuffle and ShuffleStep without errors
- Model relationships resolve without exceptions
</verification>

<success_criteria>
- Three migrations create correct schema (integer yields, unsignedBigInteger for blizzard IDs, nullable provenance FK)
- Shuffle model has user(), steps() (ordered), and deleting orphan cleanup
- ShuffleStep model has shuffle(), inputCatalogItem(), outputCatalogItem()
- User has shuffles(), WatchedItem has created_by_shuffle_id fillable and createdByShuffle()
</success_criteria>

<output>
After completion, create `.planning/phases/09-data-foundation/09-01-SUMMARY.md`
</output>
