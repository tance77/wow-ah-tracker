---
phase: 10-shuffle-crud-navigation
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - resources/views/livewire/pages/shuffle-detail.blade.php
autonomous: false
requirements: [SHUF-03, SHUF-04]

must_haves:
  truths:
    - "User can view a shuffle detail page at /shuffles/{id}"
    - "User can rename the shuffle from the detail page"
    - "User can delete the shuffle from the detail page and is redirected to /shuffles"
    - "Detail page shows profitability badge"
    - "Detail page has back link to /shuffles"
    - "Detail page has placeholder section for step editor (Phase 11)"
    - "User cannot access another user's shuffle detail page (403)"
  artifacts:
    - path: "resources/views/livewire/pages/shuffle-detail.blade.php"
      provides: "Shuffle detail shell page with rename, delete, badge, and step placeholder"
      exports: ["renameShuffle", "deleteShuffle"]
  key_links:
    - from: "resources/views/livewire/pages/shuffle-detail.blade.php"
      to: "app/Models/Shuffle.php"
      via: "mount() loads shuffle with authorization check"
      pattern: "abort_unless.*user_id.*auth"
    - from: "resources/views/livewire/pages/shuffle-detail.blade.php"
      to: "profitPerUnit()"
      via: "badge display"
      pattern: "profitPerUnit"
    - from: "resources/views/livewire/pages/shuffles.blade.php"
      to: "resources/views/livewire/pages/shuffle-detail.blade.php"
      via: "createShuffle redirect and row click navigation"
      pattern: "route\\('shuffles\\.show'"
---

<objective>
Shuffle detail shell page with rename, delete, profitability badge, and placeholder for the step editor.

Purpose: Complete the shuffle navigation flow — users click a shuffle row (or create a new one) and land on a detail page where they can rename, delete, and eventually manage steps (Phase 11).
Output: Volt SFC detail page at `/shuffles/{shuffle}` with authorization, rename, delete, badge, and back link.
</objective>

<execution_context>
@/Users/lancethompson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lancethompson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-shuffle-crud-navigation/10-CONTEXT.md
@.planning/phases/10-shuffle-crud-navigation/10-RESEARCH.md
@.planning/phases/10-shuffle-crud-navigation/10-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 that this plan depends on -->

From app/Models/Shuffle.php (after Plan 01):
```php
class Shuffle extends Model
{
    protected $fillable = ['user_id', 'name'];
    public function user(): BelongsTo;
    public function steps(): HasMany; // ordered by sort_order
    public function profitPerUnit(): ?int; // returns copper profit or null
}
```

From app/Concerns/FormatsAuctionData.php:
```php
trait FormatsAuctionData
{
    public function formatGold(int $copper): string; // handles negative values
}
```

From routes/web.php (after Plan 01):
```php
Volt::route('/shuffles/{shuffle}', 'pages.shuffle-detail')
    ->middleware(['auth'])
    ->name('shuffles.show');
```

Authorization pattern from item-detail.blade.php:
```php
public function mount(WatchedItem $watchedItem): void
{
    abort_unless($watchedItem->user_id === auth()->id(), 403);
    $this->watchedItem = $watchedItem;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shuffle detail shell page</name>
  <files>resources/views/livewire/pages/shuffle-detail.blade.php</files>
  <action>
Create `resources/views/livewire/pages/shuffle-detail.blade.php` as a Volt SFC.

**PHP class section:**
- `declare(strict_types=1);`
- `use FormatsAuctionData;` trait
- `#[Layout('layouts.app')]` attribute
- Public property: `public Shuffle $shuffle;`
- `public function mount(Shuffle $shuffle): void` — authorization check: `abort_unless($this->shuffle->user_id === auth()->id(), 403);` (same pattern as item-detail.blade.php)
- `public function renameShuffle(string $name): void` — trims and updates `$this->shuffle->name`, saves. Minimum 1 character.
- `public function deleteShuffle(): void` — calls `$this->shuffle->delete()` then `$this->redirect(route('shuffles'), navigate: true)`

**Blade template section:**
- Back link: `<a href="{{ route('shuffles') }}" wire:navigate>` with left arrow and "Back to Shuffles" text
- Page header row: shuffle name (inline editable with Alpine x-data pattern) + profitability badge (same badge markup as list page, using `$this->shuffle->profitPerUnit()` and `$this->formatGold()`)
- Inline edit: Alpine x-data `{ editing: false, saved: false }` pattern matching watchlist threshold edit. Click name to edit, Enter/blur to save via `wire:change="renameShuffle($event.target.value)"`, Escape to cancel. Show "Saved" feedback briefly.
- Step editor placeholder section: a bordered container with gray text like "Steps will be added in a future update" or similar placeholder. This section is where Phase 11 will inject the step editor.
- Delete button: styled as a danger button (red text or red border), triggers `<x-modal>` confirmation with name `confirm-delete-shuffle`.
- Modal warning text: "This will delete all steps. Auto-watched items not used by other shuffles will also be removed from your watchlist." per CONTEXT.md locked decision.
- Modal confirm button calls `wire:click="deleteShuffle"`.
- WoW dark theme: bg-wow-darker, wow-gold accents, gray-400 secondary text, consistent with other pages.
  </action>
  <verify>
    <automated>php artisan test --filter ShuffleCrud</automated>
  </verify>
  <done>Shuffle detail page renders at /shuffles/{id} with authorization check, editable name, profitability badge, delete with modal confirmation, back link to /shuffles, and placeholder for step editor. All existing tests still pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete shuffles flow</name>
  <what-built>Complete shuffles section: navigation links, list page with CRUD and profitability badges, and detail shell page with rename/delete/badge. Full flow: create shuffle from list -> lands on detail -> rename -> back to list -> delete with confirmation modal.</what-built>
  <how-to-verify>
    1. Start the dev server: `php artisan serve` (if not running)
    2. Log in and verify "Shuffles" appears in the nav bar (desktop) between Watchlist and your username
    3. Click "Shuffles" — should see empty state with explanation text and "Create Shuffle" button
    4. Click "Create Shuffle" — should redirect to `/shuffles/{id}` detail page showing "New Shuffle" as the name
    5. Click the name text on detail page — should become an editable input. Type a new name, press Enter — should save and show "Saved" briefly
    6. Click "Back to Shuffles" — should return to list showing your renamed shuffle
    7. On the list, verify the profitability badge shows a gray dot with a dash (no steps = neutral)
    8. Click the shuffle name on the list — should become editable inline. Change the name, click away — should save
    9. Click the delete button on the row — confirmation modal should appear warning about steps and auto-watched items
    10. Click "Delete" in the modal — shuffle should disappear from the list
    11. Resize browser to mobile width — verify "Shuffles" appears in the hamburger menu
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `php artisan test --filter ShuffleCrud` — all tests pass including detail page scenarios
- `php artisan test` — full suite green, no regressions
- Detail page authorization: navigating to `/shuffles/{id}` of another user's shuffle returns 403
</verification>

<success_criteria>
- Shuffle detail page renders correctly at /shuffles/{id}
- Authorization prevents access to other users' shuffles
- Rename works from detail page with inline edit pattern
- Delete from detail page removes shuffle and redirects to /shuffles
- Profitability badge displayed on detail page
- Step editor placeholder visible for Phase 11
- Human verification confirms complete end-to-end flow
</success_criteria>

<output>
After completion, create `.planning/phases/10-shuffle-crud-navigation/10-02-SUMMARY.md`
</output>
