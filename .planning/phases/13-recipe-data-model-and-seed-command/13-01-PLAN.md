---
phase: 13-recipe-data-model-and-seed-command
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/2026_03_06_200000_create_professions_table.php
  - database/migrations/2026_03_06_200001_create_recipes_table.php
  - database/migrations/2026_03_06_200002_create_recipe_reagents_table.php
  - app/Models/Profession.php
  - app/Models/Recipe.php
  - app/Models/RecipeReagent.php
  - database/factories/ProfessionFactory.php
  - database/factories/RecipeFactory.php
  - database/factories/RecipeReagentFactory.php
autonomous: true
requirements:
  - IMPORT-06

must_haves:
  truths:
    - "Running migrations creates professions, recipes, and recipe_reagents tables"
    - "Profession hasMany recipes, Recipe hasMany reagents, Recipe belongsTo profession"
    - "Deleting a profession cascades to its recipes and their reagents"
    - "Recipes have nullable crafted_item_id_silver and crafted_item_id_gold FK columns"
    - "Recipes have a last_synced_at timestamp column"
  artifacts:
    - path: "database/migrations/2026_03_06_200000_create_professions_table.php"
      provides: "professions table schema"
      contains: "create_professions_table"
    - path: "database/migrations/2026_03_06_200001_create_recipes_table.php"
      provides: "recipes table schema with dual crafted item FKs"
      contains: "crafted_item_id_silver"
    - path: "database/migrations/2026_03_06_200002_create_recipe_reagents_table.php"
      provides: "recipe_reagents table schema"
      contains: "create_recipe_reagents_table"
    - path: "app/Models/Profession.php"
      provides: "Eloquent model with recipes() relationship"
      exports: ["Profession"]
    - path: "app/Models/Recipe.php"
      provides: "Eloquent model with profession(), reagents(), craftedItemSilver(), craftedItemGold() relationships"
      exports: ["Recipe"]
    - path: "app/Models/RecipeReagent.php"
      provides: "Eloquent model with recipe(), catalogItem() relationships"
      exports: ["RecipeReagent"]
  key_links:
    - from: "app/Models/Recipe.php"
      to: "app/Models/Profession.php"
      via: "belongsTo relationship"
      pattern: "belongsTo\\(Profession::class\\)"
    - from: "app/Models/RecipeReagent.php"
      to: "app/Models/CatalogItem.php"
      via: "belongsTo relationship"
      pattern: "belongsTo\\(CatalogItem::class\\)"
---

<objective>
Create the three database tables (professions, recipes, recipe_reagents) and their Eloquent models with relationships, plus factories for testing.

Purpose: Establishes the data model that SyncRecipesCommand (Plan 02) will populate and that Phases 14-16 will query for profit calculation and display.
Output: Three migrations, three models, three factories, passing migration and relationship tests.
</objective>

<execution_context>
@/Users/lancethompson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lancethompson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-recipe-data-model-and-seed-command/13-CONTEXT.md
@.planning/phases/13-recipe-data-model-and-seed-command/13-RESEARCH.md

<interfaces>
<!-- Key types the executor needs from existing codebase -->

From app/Models/CatalogItem.php:
```php
class CatalogItem extends Model
{
    protected $fillable = ['blizzard_item_id', 'name', 'category', 'rarity', 'icon_url', 'quality_tier'];
    public function priceSnapshots(): HasMany;
}
```

From app/Models/WatchedItem.php:
```php
class WatchedItem extends Model
{
    protected $fillable = ['user_id', 'blizzard_item_id', 'name', 'buy_threshold', 'sell_threshold', 'profession', 'created_by_shuffle_id'];
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrations, models, and factories for professions, recipes, and recipe_reagents</name>
  <files>
    database/migrations/2026_03_06_200000_create_professions_table.php
    database/migrations/2026_03_06_200001_create_recipes_table.php
    database/migrations/2026_03_06_200002_create_recipe_reagents_table.php
    app/Models/Profession.php
    app/Models/Recipe.php
    app/Models/RecipeReagent.php
    database/factories/ProfessionFactory.php
    database/factories/RecipeFactory.php
    database/factories/RecipeReagentFactory.php
    tests/Feature/RecipeDataModelTest.php
  </files>
  <behavior>
    - Test 1: Migrations run successfully and tables exist (professions, recipes, recipe_reagents)
    - Test 2: Profession factory creates valid row; Recipe factory creates with profession FK
    - Test 3: Profession->recipes() returns related recipes
    - Test 4: Recipe->profession() returns parent profession
    - Test 5: Recipe->reagents() returns related RecipeReagent rows
    - Test 6: Recipe->craftedItemSilver() and ->craftedItemGold() return nullable CatalogItem
    - Test 7: RecipeReagent->catalogItem() returns CatalogItem
    - Test 8: Deleting a profession cascades to recipes and recipe_reagents
    - Test 9: recipes.last_synced_at is fillable and castable as datetime
  </behavior>
  <action>
    **Migrations:**

    1. `create_professions_table`: id, blizzard_profession_id (unsignedBigInteger, unique), name (string), icon_url (string, nullable), last_synced_at (timestamp, nullable), timestamps.

    2. `create_recipes_table`: id, profession_id (foreignId->constrained->cascadeOnDelete), blizzard_recipe_id (unsignedBigInteger, unique), name (string), crafted_item_id_silver (foreignId->nullable->constrained('catalog_items')->nullOnDelete), crafted_item_id_gold (foreignId->nullable->constrained('catalog_items')->nullOnDelete), crafted_quantity (unsignedSmallInteger, default 1), is_commodity (boolean, default true), last_synced_at (timestamp, nullable), timestamps. Index on profession_id.

    3. `create_recipe_reagents_table`: id, recipe_id (foreignId->constrained->cascadeOnDelete), catalog_item_id (foreignId->constrained->cascadeOnDelete), quantity (unsignedSmallInteger, default 1), timestamps. Index on [recipe_id, catalog_item_id].

    **Models:**

    4. `Profession` model: fillable = [blizzard_profession_id, name, icon_url, last_synced_at]. Casts: blizzard_profession_id as integer, last_synced_at as datetime. Relationship: recipes() hasMany Recipe.

    5. `Recipe` model: fillable = [profession_id, blizzard_recipe_id, name, crafted_item_id_silver, crafted_item_id_gold, crafted_quantity, is_commodity, last_synced_at]. Casts: blizzard_recipe_id as integer, crafted_quantity as integer, is_commodity as boolean, last_synced_at as datetime. Relationships: profession() belongsTo Profession, reagents() hasMany RecipeReagent, craftedItemSilver() belongsTo CatalogItem (FK crafted_item_id_silver), craftedItemGold() belongsTo CatalogItem (FK crafted_item_id_gold).

    6. `RecipeReagent` model: fillable = [recipe_id, catalog_item_id, quantity]. Casts: quantity as integer. Relationships: recipe() belongsTo Recipe, catalogItem() belongsTo CatalogItem.

    **Factories:**

    7. `ProfessionFactory`: blizzard_profession_id => fake()->unique()->numberBetween(100, 999), name => fake()->word(), icon_url => fake()->imageUrl(), last_synced_at => now().

    8. `RecipeFactory`: profession_id => Profession::factory(), blizzard_recipe_id => fake()->unique()->numberBetween(10000, 99999), name => fake()->words(3, true), crafted_item_id_silver => null, crafted_item_id_gold => null, crafted_quantity => 1, is_commodity => true, last_synced_at => now().

    9. `RecipeReagentFactory`: recipe_id => Recipe::factory(), catalog_item_id => CatalogItem::factory(), quantity => fake()->numberBetween(1, 20).

    **Tests** (write RED first, then implement):

    Create `tests/Feature/RecipeDataModelTest.php` with Pest using RefreshDatabase. Write all behavior tests listed above before creating migrations/models.

    Follow `declare(strict_types=1)` convention used across existing models.
  </action>
  <verify>
    <automated>php artisan test --filter RecipeDataModelTest</automated>
  </verify>
  <done>All three tables created by migration, all model relationships work bidirectionally, cascade delete removes child rows, factories produce valid test data, last_synced_at is present and castable</done>
</task>

</tasks>

<verification>
- `php artisan migrate:fresh` completes without error
- `php artisan test --filter RecipeDataModelTest` passes all tests
- `php artisan test` full suite remains green (no regressions)
</verification>

<success_criteria>
- Three tables exist with correct column types and FK constraints
- Three Eloquent models with all relationships defined
- Three factories produce valid test data
- Cascade delete from profession removes recipes and recipe_reagents
- All existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/13-recipe-data-model-and-seed-command/13-01-SUMMARY.md`
</output>
