---
phase: 07-dashboard-and-price-charts
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - tests/Feature/DashboardTest.php
autonomous: false
requirements: [DASH-01, DASH-02, DASH-03, DASH-06]

must_haves:
  truths:
    - "Dashboard shows only the logged-in user's watched items, not another user's"
    - "Summary cards display current price and trend direction correctly"
    - "Timeframe toggle changes chart data query boundary"
    - "Gold formatter converts copper integers to correct Xg Xs Xc format"
    - "Empty state renders when user has no watched items"
    - "No-snapshot state renders when items exist but have no price data"
  artifacts:
    - path: "tests/Feature/DashboardTest.php"
      provides: "Pest feature tests for dashboard component covering DASH-01, DASH-02, DASH-03, DASH-06"
      contains: "Volt::test"
  key_links:
    - from: "tests/Feature/DashboardTest.php"
      to: "resources/views/livewire/pages/dashboard.blade.php"
      via: "Volt::test('pages.dashboard') with actingAs()"
      pattern: "Volt::test.*pages\\.dashboard"
---

<objective>
Write comprehensive Pest feature tests for the dashboard component covering user isolation (DASH-06), summary card rendering (DASH-01), chart data loading (DASH-02), timeframe toggle (DASH-03), gold formatting, and empty/loading states. Then human-verify the full dashboard UI in a browser.

Purpose: Prove that all four phase requirements (DASH-01, DASH-02, DASH-03, DASH-06) are satisfied with automated tests, and visually confirm the dashboard works end-to-end in a real browser.

Output: Passing test suite + human verification of the complete dashboard flow.
</objective>

<execution_context>
@/Users/lancethompson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lancethompson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-dashboard-and-price-charts/07-CONTEXT.md
@.planning/phases/07-dashboard-and-price-charts/07-01-SUMMARY.md

<interfaces>
<!-- Testing patterns established in prior phases -->

Testing pattern (from Phase 3):
```php
use App\Models\User;
use App\Models\WatchedItem;
use App\Models\PriceSnapshot;
use Livewire\Volt\Volt;

it('does something', function () {
    $user = User::factory()->create();
    Volt::test('pages.dashboard')
        ->actingAs($user)
        ->assertSee('expected text');
});
```

Factory availability:
- User::factory() — standard Laravel
- WatchedItem::factory() — has user_id, blizzard_item_id, name, buy_threshold, sell_threshold
- PriceSnapshot::factory() — has watched_item_id, min_price, avg_price, median_price, total_volume, polled_at

Established test conventions:
- Pest `it()` blocks with descriptive names
- actingAs() for auth-scoped assertions
- assertSee/assertDontSee for content verification
- call() for invoking Livewire methods
- assertDispatched() for browser events
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write Pest feature tests for dashboard component</name>
  <files>tests/Feature/DashboardTest.php</files>
  <behavior>
    - Test 1 (DASH-06): User sees only their own watched items, not another user's items
    - Test 2 (DASH-01): Summary card displays item name and current median price in gold format
    - Test 3 (DASH-01): Trend arrow shows correct direction (up when current > previous, down when current < previous)
    - Test 4 (DASH-01): Items with no snapshots show "Awaiting first snapshot" message
    - Test 5 (empty state): User with no watched items sees "No items tracked yet" message
    - Test 6 (DASH-02): Selecting an item dispatches chart-data-updated event with median and min series data
    - Test 7 (DASH-03): Changing timeframe dispatches chart-data-updated event with filtered data
    - Test 8 (DASH-06): Dashboard page requires authentication (guest redirected to login)
    - Test 9 (gold format): formatGold correctly converts copper values (e.g., 1453278 = "145g 32s 78c", 50000 = "5g", 99 = "99c")
  </behavior>
  <action>
Create `tests/Feature/DashboardTest.php` with Pest feature tests.

**Test setup helpers:**
- Helper function to create a user with watched items and snapshots:
  ```php
  function createUserWithSnapshots(int $itemCount = 1, int $snapshotCount = 2): array
  {
      $user = User::factory()->create();
      $items = collect();
      foreach (range(1, $itemCount) as $i) {
          $item = WatchedItem::factory()->create(['user_id' => $user->id, 'name' => "Test Item $i"]);
          foreach (range(1, $snapshotCount) as $j) {
              PriceSnapshot::factory()->create([
                  'watched_item_id' => $item->id,
                  'polled_at' => now()->subMinutes(15 * $j),
              ]);
          }
          $items->push($item);
      }
      return [$user, $items];
  }
  ```

**Test 1 — DASH-06 user isolation:**
- Create user A with item "Bismuth", user B with item "Mycobloom".
- Acting as user A: assertSee "Bismuth", assertDontSee "Mycobloom".
- Acting as user B: assertSee "Mycobloom", assertDontSee "Bismuth".

**Test 2 — DASH-01 price display:**
- Create user with 1 item, 1 snapshot with known median_price (e.g., 1453278 copper = 145g 32s 78c).
- Assert the page contains the formatted gold string components.

**Test 3 — DASH-01 trend direction:**
- Create user with item having 2 snapshots: latest median 200000, previous median 150000.
- Assert page contains up arrow indicator (or "+" percentage text).

**Test 4 — no snapshots state:**
- Create user with 1 item, 0 snapshots.
- assertSee "Awaiting first snapshot".

**Test 5 — empty state:**
- Create user with 0 items.
- assertSee "No items tracked yet".

**Test 6 — DASH-02 chart data dispatch:**
- Create user with item + snapshots.
- Call `selectItem($item->id)` on the component.
- assertDispatched 'chart-data-updated'.

**Test 7 — DASH-03 timeframe toggle:**
- Create user with item + snapshots spanning different time ranges (some at 2 hours ago, some at 3 days ago).
- Call `setTimeframe('24h')`.
- assertDispatched 'chart-data-updated' and verify only recent snapshots are included.

**Test 8 — auth required:**
- Test GET /dashboard without auth returns redirect to login.

**Test 9 — gold formatter unit test:**
- Test the formatGold method directly via component instance or as a standalone assertion.
- Cases: 1453278 -> "145g 32s 78c", 50000 -> "5g", 99 -> "99c", 100 -> "1s", 0 -> "0c".

All tests use `declare(strict_types=1)` at the top.
Run `./vendor/bin/pint tests/Feature/DashboardTest.php` after writing.
  </action>
  <verify>
    <automated>cd /Users/lancethompson/Github/wow-ah-tracker && php artisan test --filter=DashboardTest -v 2>&1 | tail -20</automated>
  </verify>
  <done>
- All dashboard tests pass (9+ tests)
- Tests cover DASH-01, DASH-02, DASH-03, DASH-06
- User isolation proven (user A cannot see user B's items)
- Gold formatter edge cases verified
- Total test suite still passes (existing 95 + new dashboard tests)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify dashboard UI in browser</name>
  <files>N/A</files>
  <action>
What was built: Complete dashboard with summary cards, interactive ApexCharts line chart, timeframe toggle, gold/silver/copper formatting, empty states, and user isolation. All automated tests passing.

How to verify:
1. Start dev server: `php artisan serve` and `npm run dev` (in separate terminals)
2. Log in with a test account
3. **Empty state check:** If no watched items, verify "No items tracked yet" message with link to watchlist
4. **Add items:** Navigate to /watchlist, add 2-3 items, return to /dashboard
5. **Cards check:** Verify each card shows item name, price in gold format (colored text), and trend arrow
6. **Awaiting snapshot:** If items were just added and no ingestion has run, cards should show "Awaiting first snapshot"
7. **Chart check:** Click a card — verify a full-width chart panel opens below the grid with two lines (Median in gold color, Min in blue)
8. **Timeframe toggle:** Click 24h, 7d, 30d buttons — verify chart updates without page reload, active button highlighted in gold
9. **Tooltip check:** Hover over data points — verify tooltip shows timestamp, median price, and min price in gold format
10. **Deselect:** Click the same card again — verify chart panel closes
11. **User isolation:** Log in as a different user — verify they see only their own items (or empty state if no items)
12. **Responsive:** Resize browser window — verify cards reflow (3 cols -> 2 cols -> 1 col)
13. **Data freshness:** Check top of dashboard for "Updated X min ago" timestamp

Resume signal: Type "approved" or describe any issues found.
  </action>
  <verify>
    <automated>echo "Human verification checkpoint — no automated check"</automated>
  </verify>
  <done>Human confirms dashboard renders correctly: cards with prices, chart with median+min lines, timeframe toggle, responsive layout, user isolation, empty states</done>
</task>

</tasks>

<verification>
1. `php artisan test --filter=DashboardTest` — all tests pass
2. `php artisan test` — full suite passes (existing 95 + new dashboard tests)
3. Human visual verification of all dashboard states in browser
</verification>

<success_criteria>
- 9+ dashboard tests passing covering all four requirements
- Full test suite green (no regressions)
- Human confirms: cards render, chart works, timeframe toggle works, user isolation works, empty states work, gold formatting correct
</success_criteria>

<output>
After completion, create `.planning/phases/07-dashboard-and-price-charts/07-02-SUMMARY.md`
</output>
