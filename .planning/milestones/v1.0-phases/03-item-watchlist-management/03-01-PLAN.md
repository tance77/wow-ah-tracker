---
phase: 03-item-watchlist-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Models/User.php
  - app/Models/CatalogItem.php
  - database/migrations/2026_03_01_200001_create_catalog_items_table.php
  - database/migrations/2026_03_01_200002_add_unique_user_blizzard_item_to_watched_items.php
  - database/seeders/ItemCatalogSeeder.php
  - database/seeders/DatabaseSeeder.php
autonomous: true
requirements:
  - ITEM-01
  - ITEM-05

must_haves:
  truths:
    - "CatalogItem model exists and can be queried by name with SQL LIKE"
    - "User model has watchedItems() HasMany relationship"
    - "watched_items table has unique constraint on (user_id, blizzard_item_id)"
    - "Running ItemCatalogSeeder populates ~20 TWW-era crafting materials"
  artifacts:
    - path: "app/Models/CatalogItem.php"
      provides: "Eloquent model for static item catalog"
      contains: "class CatalogItem"
    - path: "app/Models/User.php"
      provides: "watchedItems relationship"
      contains: "watchedItems"
    - path: "database/migrations/2026_03_01_200001_create_catalog_items_table.php"
      provides: "catalog_items schema"
      contains: "catalog_items"
    - path: "database/migrations/2026_03_01_200002_add_unique_user_blizzard_item_to_watched_items.php"
      provides: "Unique constraint preventing duplicate watched items per user"
      contains: "unique"
    - path: "database/seeders/ItemCatalogSeeder.php"
      provides: "TWW crafting material seed data"
      contains: "ItemCatalogSeeder"
  key_links:
    - from: "app/Models/User.php"
      to: "app/Models/WatchedItem.php"
      via: "hasMany relationship"
      pattern: "hasMany.*WatchedItem"
    - from: "app/Models/CatalogItem.php"
      to: "database/migrations/2026_03_01_200001_create_catalog_items_table.php"
      via: "Eloquent convention"
      pattern: "catalog_items"
---

<objective>
Build the data layer for Phase 3: CatalogItem model with migration and seeder, User->watchedItems relationship, and unique constraint on watched_items to prevent duplicate items per user.

Purpose: The watchlist UI (Plan 02) needs a searchable catalog to add items from and a User relationship to scope queries. This plan creates all data-layer prerequisites.
Output: CatalogItem model, migration, seeder with ~20 TWW crafting materials; User model updated with watchedItems() relationship; unique index on (user_id, blizzard_item_id).
</objective>

<execution_context>
@/Users/lancethompson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lancethompson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-item-watchlist-management/03-RESEARCH.md

<interfaces>
<!-- Existing models the executor needs to understand -->

From app/Models/WatchedItem.php:
```php
class WatchedItem extends Model
{
    use HasFactory;

    protected $fillable = ['user_id', 'blizzard_item_id', 'name', 'buy_threshold', 'sell_threshold'];

    protected $casts = [
        'blizzard_item_id' => 'integer',
        'buy_threshold' => 'integer',
        'sell_threshold' => 'integer',
    ];

    public function user(): BelongsTo { return $this->belongsTo(User::class); }
    public function priceSnapshots(): HasMany { return $this->hasMany(PriceSnapshot::class); }
}
```

From app/Models/User.php (current — missing watchedItems relationship):
```php
class User extends Authenticatable
{
    use HasFactory, Notifiable;
    protected $fillable = ['name', 'email', 'password'];
    // No watchedItems() relationship yet — must be added
}
```

From database/seeders/DatabaseSeeder.php:
```php
class DatabaseSeeder extends Seeder
{
    public function run(): void
    {
        WatchedItem::factory()->count(5)->has(PriceSnapshot::factory()->count(20))->create();
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CatalogItem model, migration, and seeder with TWW crafting materials</name>
  <files>app/Models/CatalogItem.php, database/migrations/2026_03_01_200001_create_catalog_items_table.php, database/seeders/ItemCatalogSeeder.php, database/seeders/DatabaseSeeder.php</files>
  <action>
1. Create migration `database/migrations/2026_03_01_200001_create_catalog_items_table.php`:
   - `$table->id()`
   - `$table->unsignedBigInteger('blizzard_item_id')->unique()`
   - `$table->string('name')`
   - `$table->string('category')` — values: herb, ore, cloth, leather, enchanting, gem
   - `$table->timestamps()`

2. Create `app/Models/CatalogItem.php`:
   - `declare(strict_types=1);`
   - Namespace `App\Models`
   - Extends `Model`, uses `HasFactory`
   - `$fillable = ['blizzard_item_id', 'name', 'category']`
   - `$casts = ['blizzard_item_id' => 'integer']`
   - No relationships needed

3. Create `database/seeders/ItemCatalogSeeder.php`:
   - `declare(strict_types=1);`
   - Seeds ~20 TWW-era crafting materials using `CatalogItem::updateOrCreate()` (idempotent on blizzard_item_id)
   - Categories and items from RESEARCH.md:
     - Herbs: Luredrop (222788), Orbinid (222785), Blessing Blossom (222790), Gundegrass (222789), Arathor's Spear (222791), Ironcap Mushroom (222792)
     - Ores: Uldricite (224023), Bismuth (224025), Ironcite (224024)
     - Cloth: Weavercloth (224570), Dawnweave (224569)
     - Leather: Amplified Monstrous Hide (224552), Monstrous Hide (224551)
     - Enchanting: Resonant Crystal (222825), Spark of Omens (222826), Glittering Parchment (222823)
     - Gems: Irradiated Taaffeite (220156), Irradiated Ruby (220153), Irradiated Emerald (220155)
   - Add comment: "Item IDs are TWW-era placeholders — verify against live Blizzard API in Phase 4"

4. Update `database/seeders/DatabaseSeeder.php`:
   - Add `$this->call(ItemCatalogSeeder::class);` BEFORE the existing WatchedItem factory calls
   - Keep existing WatchedItem + PriceSnapshot seeding intact

5. Run `php artisan migrate` to create catalog_items table.
6. Run `php artisan db:seed --class=ItemCatalogSeeder` to verify seeder works.
7. Run `vendor/bin/pint` on all new and modified files.
  </action>
  <verify>
    <automated>cd /Users/lancethompson/Github/wow-ah-tracker && php artisan migrate --force && php artisan db:seed --class=ItemCatalogSeeder && php artisan tinker --execute="echo App\Models\CatalogItem::count();" | grep -E "^[12][0-9]$"</automated>
  </verify>
  <done>CatalogItem model exists, migration creates catalog_items table, seeder populates ~20 items, DatabaseSeeder calls ItemCatalogSeeder</done>
</task>

<task type="auto">
  <name>Task 2: Add watchedItems relationship to User model and unique constraint on watched_items</name>
  <files>app/Models/User.php, database/migrations/2026_03_01_200002_add_unique_user_blizzard_item_to_watched_items.php</files>
  <action>
1. Edit `app/Models/User.php`:
   - Add `use Illuminate\Database\Eloquent\Relations\HasMany;` import
   - Add `use App\Models\WatchedItem;` import (or use FQCN in return type)
   - Add method:
     ```php
     public function watchedItems(): HasMany
     {
         return $this->hasMany(WatchedItem::class);
     }
     ```
   - Keep all existing code intact (fillable, hidden, casts, HasFactory, Notifiable)

2. Create migration `database/migrations/2026_03_01_200002_add_unique_user_blizzard_item_to_watched_items.php`:
   - Add unique composite index on `['user_id', 'blizzard_item_id']` to `watched_items` table
   - `$table->unique(['user_id', 'blizzard_item_id']);`
   - Down method drops the unique index

3. Run `php artisan migrate` to apply the unique constraint.
4. Run `vendor/bin/pint` on modified files.

NOTE: The WatchedItem model already has `user_id` in $fillable and a `user()` BelongsTo relationship. This task completes the inverse side on User.
  </action>
  <verify>
    <automated>cd /Users/lancethompson/Github/wow-ah-tracker && php artisan migrate --force && php artisan tinker --execute="echo method_exists(App\Models\User::class, 'watchedItems') ? 'OK' : 'FAIL';" | grep OK</automated>
  </verify>
  <done>User::watchedItems() returns HasMany relationship; unique constraint on (user_id, blizzard_item_id) prevents duplicate items per user</done>
</task>

</tasks>

<verification>
1. `php artisan migrate:status` shows both new migrations as "Ran"
2. `CatalogItem::where('name', 'like', '%ore%')->count()` returns > 0
3. `User::factory()->create()->watchedItems()` returns a HasMany query builder
4. Attempting to insert duplicate (user_id, blizzard_item_id) throws unique constraint violation
</verification>

<success_criteria>
- CatalogItem model with migration and ~20 seeded TWW items
- User model has watchedItems() HasMany relationship
- watched_items table has unique constraint on (user_id, blizzard_item_id)
- All files pass Pint (declare_strict_types enforced)
- All migrations run cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/03-item-watchlist-management/03-01-SUMMARY.md`
</output>
